Get the list of numbers that already generated by the Publish Numbers service
---
parameters:
  - name: offset
    in: path
    type: number
    default: 0
    description: The offset of number
  - name: limit
    in: path
    type: number
    default: 20
    description: The limit of numbers per page
definitions:
  List:
    type: object
    properties:
      count:
        type: number
        description: The maximum number of numbers that exists in database
      data:
        type: array
        description: The list of numbers
        items:
          type: number
          description: The number tha was generated
responses:
  200:
    description: A list of numbers
    schema:
      $ref: "#/definitions/List"
